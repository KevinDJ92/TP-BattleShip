package utils;

import java.util.ArrayList;

import models.Grid;
import views.principals.GameButton;

public class Positionnement {

	final static int nbCase = 100;
	
	public static Grid GenerateGrid(ArrayList<GameButton> gameButtons) {
	
		Grid grid = new Grid(gameButtons);

		return grid;
	}	
}
